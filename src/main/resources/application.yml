server:
  port: 80
spring:
  profiles:
    active: local
    include:
      - secret
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  messages:
    basename: i18n/messages
    encoding: UTF-8
    cache-duration: PT1H
